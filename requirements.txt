# Версии проверены совместно: pip разрешает их без конфликтов.
#
# ВАЖНО про anthropic: версия 0.40.0, стоявшая раньше, НЕ умеет кеширование
# промпта — в её объекте Usage нет полей cache_creation_input_tokens и
# cache_read_input_tokens, а параметр cache_control ещё был бета-функцией.
# На ней вся экономия на себестоимости не работала бы, причём молча.
anthropic==0.121.0

aiogram==3.13.1
openai==2.53.0
httpx==0.28.1

# Postgres вместо прежней SQLite: контейнер эфемерный, файл базы теряется
# при каждом деплое.
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
